#include <stdio.h>

int main() {
    int num1 = 10;
    int num2 = 20;

    if (num1 > num2) {
        printf("%d es mayor que %d\n", num1, num2);
    } else {
        printf("%d es menor que %d\n", num1, num2);
        
    }

    return 0;
}